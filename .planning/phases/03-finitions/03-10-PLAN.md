---
phase: 03-finitions
plan: 10
type: execute
wave: 3
depends_on: [03-09]
files_modified: [tests/e2e/score-calculation.spec.ts]
autonomous: true
requirements: [REQ-06]

must_haves:
  truths:
    - "The application passes all react-doctor diagnostic checks."
    - "Playwright E2E tests verify the complete flow from grade entry to admission chance visibility."
  artifacts:
    - path: "tests/e2e/score-calculation.spec.ts"
      provides: "Expanded E2E test coverage for score-to-admission-chance flow"
  key_links:
    - from: "tests/e2e/score-calculation.spec.ts"
      to: "LyceesIndicateurs"
      via: "assertion of admission chance visibility"
---

<objective>
Perform final code quality verification and expand E2E test coverage to ensure a reliable user experience.
Purpose: Catch any remaining performance issues or regressions and verify the end-to-end integration.
Output: Report of successful react-doctor run and a more comprehensive E2E test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e/score-calculation.spec.ts
@src/components/LyceeDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run diagnostics with react-doctor</name>
  <files>.</files>
  <action>
    - Run `npx react-doctor@latest .` to identify any React-specific issues (leaks, anti-patterns).
    - Fix any reported errors or warnings in the newly added components.
  </action>
  <verify>
    <automated>npx react-doctor@latest .</automated>
  </verify>
  <done>React-doctor reports no issues in the codebase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand Playwright E2E tests</name>
  <files>tests/e2e/score-calculation.spec.ts</files>
  <behavior>
    - User calculates score in Score tab.
    - User searches for a sector (address or college).
    - User clicks on a lycée to expand details.
    - User verifies that "Chances d'admission" is visible and reflects the calculated score.
  </behavior>
  <action>
    - Update `tests/e2e/score-calculation.spec.ts`:
        - Mock the thresholds API (seuilsApi.ts).
        - Add a test case for the full "Score calculation + Lycée detail comparison" flow.
        - Ensure data persistence is also verified (reloading page preserves score and admission status visibility).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/score-calculation.spec.ts</automated>
  </verify>
  <done>Playwright tests cover the full end-to-end integration of the score calculation and lycée comparison.</done>
</task>

</tasks>

<verification>
- Verify that all Playwright tests pass in the CI environment (local run).
- Verify that react-doctor is satisfied with the codebase structure.
</verification>

<success_criteria>
- [ ] react-doctor output is clean.
- [ ] Playwright coverage includes the admission chance verification.
- [ ] No regressions in core sector search functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/03-finitions/03-10-SUMMARY.md`
</output>
