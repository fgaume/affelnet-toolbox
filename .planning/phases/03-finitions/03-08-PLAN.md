---
phase: 03-finitions
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: [src/App.tsx, src/components/CollegeCard.tsx, src/components/LyceeDetail.tsx]
autonomous: true
requirements: [REQ-5-INTEG]

must_haves:
  truths:
    - "Lycée details show admission chances based on the user's score."
    - "Admission chance text is updated when the user changes their grades."
    - "Threshold comparison uses the most recent 2025 thresholds from seuilsApi.ts."
  artifacts:
    - path: "src/components/LyceeDetail.tsx"
      provides: "Admission chance display in LyceesIndicateurs"
      contains: "userScore"
    - path: "src/components/CollegeCard.tsx"
      provides: "Score propagation from App down to LyceeDetail"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/CollegeCard.tsx"
      via: "score prop"
    - from: "src/components/CollegeCard.tsx"
      to: "src/components/LyceeDetail.tsx"
      via: "userScore prop"
    - from: "src/components/LyceeDetail.tsx"
      to: "src/services/seuilsApi.ts"
      via: "fetchSeuils call"
---

<objective>
Update the lycée details view to display admission chances when a user score is available.
Purpose: Provide users with a dynamic comparison between their calculated score and historical admission thresholds.
Output: Dynamic admission status (Élevée / Faible / Inconnue) in the LyceesIndicateurs component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.tsx
@src/components/CollegeCard.tsx
@src/components/LyceeDetail.tsx
@src/services/seuilsApi.ts

<interfaces>
From src/types/index.ts:
```typescript
export interface UserScore {
  finalScore: number;
  disciplinaryFields: Record<DisciplinaryField, ScoreBreakdown>;
}
```

From src/services/seuilsApi.ts:
```typescript
export async function fetchSeuils(): Promise<Map<string, number>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Propagate score state down the component tree</name>
  <files>src/App.tsx, src/components/CollegeCard.tsx, src/components/LyceeDetail.tsx</files>
  <action>
    - Update App.tsx: Pass the `score` state as a `userScore` prop to the `CollegeCard` component when `result` is present.
    - Update src/components/CollegeCard.tsx:
        - Add `userScore?: UserScore | null` to `CollegeCardProps`.
        - Pass `userScore` as a prop to `LyceesIndicateurs`.
    - Update src/components/LyceeDetail.tsx:
        - Add `userScore?: UserScore | null` to `LyceesIndicateursProps`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The userScore prop is correctly passed from App.tsx down to LyceesIndicateurs without type errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement admission chance calculation and display</name>
  <files>src/components/LyceeDetail.tsx</files>
  <behavior>
    - If userScore is null: Display nothing additional.
    - If userScore exists: Fetch thresholds using `fetchSeuils`.
    - Compare `userScore.finalScore` with the lycée threshold:
        - Score >= threshold: Show "Chances d'admission: Élevée" (green color).
        - Score < threshold: Show "Chances d'admission: Faible" (red color).
        - No threshold for lycée: Show "Seuil inconnu".
  </behavior>
  <action>
    - In `LyceesIndicateurs`, add a `useEffect` to fetch thresholds when `userScore` is present.
    - Store thresholds in local state.
    - In the rendering logic for each lycée, calculate the admission status.
    - Add a small text or badge above or near the charts for each lycée (or in the legend area) displaying the admission chance.
    - Ensure it updates when `userScore` changes (it should, as it's a prop).
  </action>
  <verify>
    <automated>npm test src/services/__tests__/seuilsApi.test.ts</automated>
  </verify>
  <done>Admission chances are visible in the lycée detail view when a score is calculated.</done>
</task>

</tasks>

<verification>
- Check that admission chances appear in `LyceeDetail` after a score is calculated in the "Calculer son score" tab.
- Check that switching back to the "Par adresse" tab and looking at the lycée list shows the status.
</verification>

<success_criteria>
- [ ] userScore is passed from App.tsx to LyceesIndicateurs.
- [ ] LyceesIndicateurs displays admission status (Élevée/Faible) based on 2025 thresholds.
- [ ] Status updates dynamically when grades are changed in the score tab.
</success_criteria>

<output>
After completion, create `.planning/phases/03-finitions/03-08-SUMMARY.md`
</output>
