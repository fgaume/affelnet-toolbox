---
phase: 03-finitions
plan: 09
type: execute
wave: 2
depends_on: [03-08]
files_modified: [src/index.css, src/components/GradeInputForm.css, src/components/ScoreDisplay.css, src/components/GradeInputForm.tsx, src/components/ScoreDisplay.tsx]
autonomous: true
requirements: [REQ-04, REQ-06]

must_haves:
  truths:
    - "Application color theme is consistent between core app and score components."
    - "Score components are usable and readable in both light and dark modes."
    - "Form inputs and tables are accessible for assistive technologies."
  artifacts:
    - path: "src/components/GradeInputForm.css"
      provides: "Harmonized CSS variables for grade input form"
    - path: "src/components/ScoreDisplay.css"
      provides: "Harmonized CSS variables for score display"
  key_links:
    - from: "src/components/GradeInputForm.css"
      to: "src/index.css"
      via: "standard CSS variable names"
---

<objective>
Harmonize CSS variables across the application and polish the UI/UX for better consistency and accessibility.
Purpose: Ensure a professional and unified look, particularly for the new score calculation components.
Output: Refactored CSS files using standard variables and improved accessibility markup.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.css
@src/components/GradeInputForm.css
@src/components/ScoreDisplay.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize CSS variables</name>
  <files>src/components/GradeInputForm.css, src/components/ScoreDisplay.css</files>
  <action>
    - Refactor `src/components/GradeInputForm.css`:
        - Replace `--bg-card` with `--color-bg-card`
        - Replace `--border-color` with `--color-border`
        - Replace `--text-primary` with `--color-text-primary`
        - Replace `--text-secondary` with `--color-text-secondary`
        - Replace `--primary-color` with `--color-primary`
        - Replace `--bg-subtle` with `--color-bg-subtle`
    - Refactor `src/components/ScoreDisplay.css`:
        - Replace `--card-bg` with `--color-bg-card`
        - Replace `--bg-secondary` with `--color-bg-subtle`
        - Replace `--info-bg` with `--color-info-bg`
    - Ensure all remaining hardcoded colors or inconsistent variables are replaced with standard ones from `index.css`.
  </action>
  <verify>
    <automated>grep -E "(--bg-card|--border-color|--text-primary|--card-bg)" src/components/GradeInputForm.css src/components/ScoreDisplay.css || echo "All harmonized"</automated>
  </verify>
  <done>Component-specific CSS now exclusively uses variables defined in :root in index.css.</done>
</task>

<task type="auto">
  <name>Task 2: Accessibility improvements</name>
  <files>src/components/GradeInputForm.tsx, src/components/ScoreDisplay.tsx</files>
  <action>
    - Add `aria-label` to grade inputs to provide more context (e.g., "Moyenne annuelle de Français").
    - Ensure `ScoreDisplay` table uses `scope="col"` on headers.
    - Add descriptive `alt` or `aria-label` to any visual icons used in the score components.
    - Check that color contrast is maintained for both themes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Accessibility attributes (ARIA) are present on key interactive and informative elements.</done>
</task>

</tasks>

<verification>
- Toggle dark mode and ensure the score tab components are correctly styled.
- Check that focus rings and labels are clear for keyboard navigation.
</verification>

<success_criteria>
- [ ] No inconsistent CSS variables (like --bg-card instead of --color-bg-card).
- [ ] Dark mode is fully supported for the score tab.
- [ ] ARIA labels present on grade inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-finitions/03-09-SUMMARY.md`
</output>
