---
phase: 01-logic
plan: 03
type: execute
wave: 3
depends_on: [01-logic-01, 01-logic-02]
files_modified: [src/services/storage.ts]
autonomous: true
requirements: [REQ-05, REQ-06]
must_haves:
  truths:
    - "User grades are persisted in localStorage"
    - "Calculated scores are persisted in localStorage"
    - "Clearing data removes both grades and scores"
  artifacts:
    - path: "src/services/storage.ts"
      provides: "Methods for scores and grades persistence"
---

<objective>
Extend the storage service to persist user grades and calculated scores in the browser's local storage.

Purpose: Allow users to return to the app and find their previously entered grades and calculated scores.
Output: Updated storage service with grades and score persistence.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/types/index.ts
@src/services/storage.ts
@.planning/phases/01-logic/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend storage.ts for Grades and Scores</name>
  <files>src/services/storage.ts</files>
  <action>
    Add the following functions to `src/services/storage.ts`:
    - `getUserGrades()`: Retrieves grades from localStorage (`affelnet-user-grades`).
    - `saveUserGrades(grades: UserGrades)`: Saves grades to localStorage.
    - `getUserScore()`: Retrieves calculated score from localStorage (`affelnet-user-score`).
    - `saveUserScore(score: UserScore)`: Saves score to localStorage.
    - `clearScoreData()`: Clears both grades and score data.
    
    Ensure types from `src/types/index.ts` are used for these functions.
    Maintain existing search history functionality in `storage.ts`.
  </action>
  <verify>
    <automated>node -e "import('./src/services/storage.ts').then(m => { const g = { FRANCAIS: 15 }; m.saveUserGrades(g); console.log(m.getUserGrades()); })"</automated>
  </verify>
  <done>Grades and scores can be saved and retrieved from localStorage.</done>
</task>

</tasks>

<verification>
Verify that saving grades and scores works by checking the `localStorage` content or using the implemented getter functions.
</verification>

<success_criteria>
- Grades and scores are persisted across page reloads.
- No regression on existing search history persistence.
- Data structures match the defined TypeScript types.
</success_criteria>

<output>
After completion, create `.planning/phases/01-logic/01-03-SUMMARY.md`
</output>
