---
phase: 01-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/index.ts, src/services/scoreApi.ts]
autonomous: true
requirements: [REQ-03, REQ-06]
must_haves:
  truths:
    - "Academic statistics (mu/sigma) are fetched for the 7 fields"
    - "The latest year from the Hugging Face dataset is automatically selected"
    - "TypeScript interfaces cover all calculation data structures"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Common types for calculation"
    - path: "src/services/scoreApi.ts"
      provides: "Academic statistics fetching"
      exports: ["fetchAcademicStats"]
  key_links:
    - from: "src/services/scoreApi.ts"
      to: "Hugging Face API"
      via: "fetch"
---

<objective>
Define the data foundation for the Affelnet score calculation by creating the necessary TypeScript types and implementing the API service to fetch academic statistics (mu/sigma) from Hugging Face.

Purpose: Ensure type safety across the calculation logic and provide a reliable way to get the latest harmonization parameters.
Output: Updated types and a functional score API service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Score Calculation Types</name>
  <files>src/types/index.ts</files>
  <action>
    Add the following types and constants to `src/types/index.ts`:
    - `Subject`: Literal type for the 12 subjects (FRANCAIS, MATHEMATIQUES, HISTOIRE_GEO, LV1, LV2, EPS, ARTS_PLASTIQUES, EDUCATION_MUSICALE, SVT, TECHNOLOGIE, PHYSIQUE_CHIMIE, EMC).
    - `DisciplinaryField`: Literal type for the 7 fields (FRANCAIS, MATHEMATIQUES, HISTOIRE_GEO, LANGUES_VIVANTES, SCIENCES_TECHNO_DP, ARTS, EPS).
    - `UserGrades`: Record mapping `Subject` to `number | null`.
    - `AcademicStats`: Object with `moyenne` (mu) and `ecartType` (sigma) for a field.
    - `ScoreDetail`: Object with `rawAverage`, `harmonizedNote`, and `contribution` for a field.
    - `UserScore`: Object with `totalScore` and `details` (Record of DisciplinaryField to ScoreDetail).
    - `AcademicStatsResponse`: Type for the Hugging Face API response structure.
    
    Ensure all exports are properly placed.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Types are defined and project still compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scoreApi.ts</name>
  <files>src/services/scoreApi.ts</files>
  <action>
    Create `src/services/scoreApi.ts` to fetch mu/sigma statistics:
    - Target URL: `https://datasets-server.huggingface.co/rows?dataset=fgaume%2Faffelnet-paris-statistiques-champs-disciplinaires&config=default&split=train&offset=0&length=100`
    - Method `fetchAcademicStats()`:
      - Fetches data from the Hugging Face API.
      - Identifies the most recent year (`annee`) in the dataset.
      - Maps the rows for that year to a `Record<DisciplinaryField, AcademicStats>`.
      - Handles potential fetch errors with descriptive messages.
    - Export the function.
  </action>
  <verify>
    <automated>node -e "import('./src/services/scoreApi.ts').then(m => m.fetchAcademicStats().then(console.log))"</automated>
  </verify>
  <done>Stats are fetched and mapped correctly from the API.</done>
</task>

</tasks>

<verification>
Check that `src/types/index.ts` contains all necessary interfaces and that `scoreApi.ts` can successfully retrieve and parse the academic statistics.
</verification>

<success_criteria>
- TypeScript types cover all aspects of the score calculation.
- `fetchAcademicStats` returns a record of mu/sigma for the 7 disciplinary fields for the latest year available.
</success_criteria>

<output>
After completion, create `.planning/phases/01-logic/01-01-SUMMARY.md`
</output>
