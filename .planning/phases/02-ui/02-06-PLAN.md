---
phase: 02-ui
plan: 06
type: execute
wave: 2
depends_on: [02-05]
files_modified: [src/components/ScoreDisplay.tsx, src/components/ScoreDisplay.css, src/components/index.ts]
autonomous: true
requirements: [4, 5]
---

<objective>
Implement the ScoreDisplay component to present the final score and a detailed breakdown.
The component should visualize the results from the `calculateAffelnetScore` function.

Purpose: Provide the user with clear information about their calculated score and how it was derived.
Output: ScoreDisplay component with total score and breakdown table.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ui/02-RESEARCH.md
@src/types/index.ts
@src/services/scoreCalculation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ScoreDisplay UI structure</name>
  <files>src/components/ScoreDisplay.tsx, src/components/ScoreDisplay.css</files>
  <action>
    - Create `ScoreDisplay.tsx` and `ScoreDisplay.css`.
    - Component props should accept `score: UserScore | null`.
    - Display the total score prominently at the top with a large numeric value.
    - If score is null (initial state), show a placeholder message "Saisissez vos notes pour calculer votre score".
    - Export the component from `src/components/index.ts`.
  </action>
  <verify>
    <automated>ls src/components/ScoreDisplay.tsx src/components/ScoreDisplay.css</automated>
  </verify>
  <done>Component files created and basic total score display implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement breakdown table</name>
  <files>src/components/ScoreDisplay.tsx</files>
  <action>
    - Render a table showing the detail for each of the 7 `DisciplinaryField`.
    - Columns: Field Name, Moyenne (raw average), Points (harmonized score * coefficient), Contribution (percentage of total score).
    - Format numeric values to 2 decimal places for raw average and 0 decimal places for points/total.
    - Add a brief explanation of score harmonization below the table (e.g., "Harmonisation académique basée sur la moyenne et l'écart-type").
    - Use Recharts to add a simple visual bar chart or progress gauge for the total score (max total score is 38,400 points).
  </action>
  <verify>
    <automated>grep -q "table" src/components/ScoreDisplay.tsx && grep -q "details" src/components/ScoreDisplay.tsx</automated>
  </verify>
  <done>Detailed breakdown table and visual chart implemented.</done>
</task>

</tasks>

<verification>
Ensure the total score matches the sum of individual field contributions * 2.
Verify table columns show correct values from UserScore object.
</verification>

<success_criteria>
- Total score is prominently displayed.
- Detailed breakdown table is visible and correctly formatted.
- Explanation of calculation is included.
- Responsive design for mobile (table scrolling or stacking).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui/02-06-SUMMARY.md`
</output>
