---
phase: 02-ui
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [src/components/GradeInputForm.tsx, src/components/GradeInputForm.css, src/components/index.ts]
autonomous: true
requirements: [2, 5]
---

<objective>
Create the GradeInputForm component to allow users to enter their 12 subject grades.
The form should group subjects into 7 disciplinary fields for better UX and alignment with the calculation logic.

Purpose: Enable users to provide the necessary data for score calculation.
Output: GradeInputForm component with validation and persistence.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ui/02-RESEARCH.md
@src/types/index.ts
@src/services/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GradeInputForm component structure</name>
  <files>src/components/GradeInputForm.tsx, src/components/GradeInputForm.css</files>
  <action>
    - Create `GradeInputForm.tsx` and `GradeInputForm.css`.
    - Define a `FIELD_MAPPING` internal constant to group `Subject` into `DisciplinaryField` (use mapping from `scoreCalculation.ts`).
    - Use local state to manage the 12 grades (`UserGrades` type).
    - Render 7 sections, one for each `DisciplinaryField`.
    - Inside each section, render an input for each subject in that field.
    - Inputs should be `type="number"`, `step="0.01"`, `min="0"`, `max="20"`.
    - Use `inputmode="decimal"` for mobile friendliness (per RESEARCH.md).
  </action>
  <verify>
    <automated>ls src/components/GradeInputForm.tsx src/components/GradeInputForm.css</automated>
  </verify>
  <done>Component files created with correct structure and groupings.</done>
</task>

<task type="auto">
  <name>Task 2: Implement validation and persistence</name>
  <files>src/components/GradeInputForm.tsx</files>
  <action>
    - Implement `onChange` handler that validates values are between 0 and 20.
    - Load initial grades from `localStorage` using `getGrades()` from `src/services/storage.ts` in a `useEffect` on mount.
    - Save grades to `localStorage` using `saveGrades()` whenever they change.
    - Add a "Réinitialiser" button to clear all grades (set to `null`) and clear storage.
    - Export the component from `src/components/index.ts`.
  </action>
  <verify>
    <automated>grep -q "saveGrades" src/components/GradeInputForm.tsx && grep -q "getGrades" src/components/GradeInputForm.tsx</automated>
  </verify>
  <done>Grades are persisted across reloads and validated on input.</done>
</task>

</tasks>

<verification>
Check if all 12 subjects are present and grouped into the 7 fields.
Verify that entering a grade saves it to localStorage.
</verification>

<success_criteria>
- 12 input fields are rendered.
- Fields are grouped by disciplinary field.
- Validation prevents values outside 0-20.
- Grades persist after page reload (once integrated).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui/02-05-SUMMARY.md`
</output>
