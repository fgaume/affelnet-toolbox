---
phase: 02-ui
plan: 07
type: execute
wave: 3
depends_on: [02-05, 02-06]
files_modified: [src/App.tsx, src/App.css, src/types/index.ts]
autonomous: true
requirements: [1, 5]
---

<objective>
Integrate the GradeInputForm and ScoreDisplay components into the main application.
Provide a dedicated tab for score calculation and manage the orchestration of data fetching (stats) and score calculation.

Purpose: Provide a complete, integrated experience for users to calculate their scores.
Output: Integrated "Score" tab in App.tsx.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ui/02-RESEARCH.md
@src/types/index.ts
@src/App.tsx
@src/services/scoreApi.ts
@src/services/scoreCalculation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navigation and state in App.tsx</name>
  <files>src/App.tsx, src/types/index.ts</files>
  <action>
    - Update `InputMode` type in `src/types/index.ts` to include `'score'`.
    - Update `App.tsx` to add the "Calculer son score" tab button in the header.
    - Use an appropriate SVG icon (e.g., calculator or clipboard icon) for the new tab.
    - Implement the logic to switch to `inputMode === 'score'`.
  </action>
  <verify>
    <automated>grep -q "'score'" src/types/index.ts && grep -q "score" src/App.tsx</automated>
  </verify>
  <done>Navigation supports the new "Score" tab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Score components and orchestration</name>
  <files>src/App.tsx</files>
  <behavior>
    - When "Score" tab is active, GradeInputForm and ScoreDisplay are visible.
    - Changing grades in GradeInputForm triggers recalculation in App.tsx (or via hook if preferred).
    - Academic stats are fetched on mount (or when entering tab) via `fetchAcademicStats()`.
  </behavior>
  <action>
    - Create a state `score: UserScore | null` in `App.tsx`.
    - Fetch academic stats using `fetchAcademicStats` on component mount or when the score tab is activated.
    - Implement a `handleGradesChange` callback that calls `calculateAffelnetScore` and updates the `score` state.
    - Render `GradeInputForm` and `ScoreDisplay` side-by-side (desktop) or stacked (mobile).
  </action>
  <verify>
    <automated>grep -q "fetchAcademicStats" src/App.tsx && grep -q "calculateAffelnetScore" src/App.tsx</automated>
  </verify>
  <done>Form and display are integrated and functional.</done>
</task>

</tasks>

<verification>
Check that clicking the "Score" tab shows the input form and score results.
Verify that changing a grade immediately updates the total score (if stats are loaded).
Ensure responsiveness of the new view.
</verification>

<success_criteria>
- Navigation tab for "Score" works correctly.
- Academic stats are loaded successfully from the API.
- Grades entry and score calculation are seamlessly integrated.
- UI remains responsive and theme-compliant (light/dark mode).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui/02-07-SUMMARY.md`
</output>
